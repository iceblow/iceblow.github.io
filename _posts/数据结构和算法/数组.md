### 数组

数组是最常见也是最基础的数据结构。

#### 什么是数组

数组（Array）是一种线性表数据结构，它用一组连续的内存空间，来存储固定大小且相同类型的数据。

##### 线性表

线性表就是数据排成像一条线一样的结构。每个线性表上的数据最多只有前和后两个方向。其实除了数组，链表、队列、栈等也是线性表结构。

非线性表，比如二叉树、堆、图等。之所以叫非线性，是因为数据之间并不是简单的前后关系。

##### 连续的内存空间、固定大小、同类型

特性：随机访问

优点：根据下标随机访问效率高

缺点：数组中删除、插入一个数据，为了保证连续性，就需要做大量的数据搬移工作。

#### 如何根据下标查找元素？

寻址公式如下：

```java
a[i]_address = base_address + i * data_type_size
```

其中 base_address是内存块的首地址，data_type_size 表示数组中每个元素的字节大小。

##### 数组下标从0开始而不是1

从 1 开始编号，每次随机访问数组元素都多了一次减法运算，对于 CPU 来说，就是多了一次减法指令。

#### 插入

#### 删除

